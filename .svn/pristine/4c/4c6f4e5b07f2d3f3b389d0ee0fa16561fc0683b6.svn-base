unit CountyController;

County[] getAllCounties(){
	return County:all();
}

County[] getCountiesForName(string name){
	return County:equals(countyName, name);
}

County getCountyForName(string name){
	County county;
	County[] counties = getCountiesForName(name);

	if (counties.length() > 0){
		county = counties.get(0);

		if (counties.length() > 1){
			string msg = String:concat("WARNING! There are duplications for the County: ", name);
			Alerter:alert(msg);
			Logger:log(msg);
		}

	}

	return county;

}


bool doesCountyHaveSubCounties(County county){

	SubCounty[] subCounties = county.subCounties;

	if (subCounties.length() >= 1){
		return true;
	}

	return false;

}