unit ManageClientPatternStart;

Client client;
Pattern pattern;
Topic topic;

void init() {
	client = Client:new();
	pattern = Pattern:new();
	topic = Topic:new();
}

void startStartClientPattern() {

	PatternList:initSMSPattern(client, pattern );
	
}

void selectClient() {
	int i =0;
	Mez:alert("client.selected");
}

void selectPattern() {
	int i = 0;
	Mez:alert("pattern.selected");
}

void selectTopic() {
	int i = 0;
	Mez:alert("pattern.selected");
}

void startStartClientTopic() {
	string st = Strings:concat("startStartClientTopic In ManagePatternStart - Removed - Client: ",client.mobileNumber, ", Topic: " ,topic.topicID,", Starting Topic"); Mez:log(st);
	//TopicList:startTopic(client, topic );
}

void startEndClientTopic() {
    string st = Strings:concat("startEndClientTopic Client ",client.mobileNumber, "Topic " ,topic.topicID); Mez:log(st);
	//TopicList:endTopic(client, topic );	
}